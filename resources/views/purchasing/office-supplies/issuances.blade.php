@extends('layouts.app')

@section('page_title', 'Office Supply Issuances')
@section('content')
<div class="card mb-3"><div class="card-header">Create Issuance</div><div class="card-body"><form method="POST" action="{{ route('office-supplies.issuances.store') }}" class="form-row">@csrf<div class="col-md-2 mb-2"><select name="branch_id" class="form-control" required><option value="">Branch</option>@foreach($branches as $branch)<option value="{{ $branch->id }}">{{ $branch->branch_name ?? $branch->name }}</option>@endforeach</select></div><div class="col-md-2 mb-2"><select name="issued_to" class="form-control" required><option value="">Employee</option>@foreach($users as $user)<option value="{{ $user->id }}">{{ $user->full_name ?? $user->name }}</option>@endforeach</select></div><div class="col-md-2 mb-2"><input type="date" name="issue_date" class="form-control" value="{{ now()->toDateString() }}" required></div><div class="col-md-2 mb-2"><input name="purpose" class="form-control" placeholder="Purpose" required></div><div class="col-md-3 mb-2"><select name="items[0][office_supply_id]" class="form-control" required><option value="">Supply</option>@foreach($supplies as $supply)<option value="{{ $supply->id }}">{{ $supply->supply_name }}</option>@endforeach</select></div><div class="col-md-1 mb-2"><input type="number" min="1" name="items[0][quantity_requested]" class="form-control" placeholder="Qty" required></div><div class="col-md-2 mb-2"><button class="btn btn-success btn-block">Submit</button></div></form></div></div>
<div class="card"><div class="card-header">Issuance Log</div><div class="table-responsive p-0"><table class="table table-sm mb-0"><thead><tr><th>Issuance #</th><th>Branch</th><th>Issued To</th><th>Date</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($issuances as $row)<tr><td>{{ $row->issuance_number }}</td><td>{{ $row->branch?->branch_name ?? $row->branch?->name }}</td><td>{{ $row->recipient?->full_name ?? $row->recipient?->name }}</td><td>{{ $row->issue_date }}</td><td>{{ ucfirst($row->status) }}</td><td>@if($row->status === 'pending')<form method="POST" action="{{ route('office-supplies.issuances.approve', $row) }}" class="d-inline">@csrf<button class="btn btn-sm btn-outline-success">Approve</button></form><form method="POST" action="{{ route('office-supplies.issuances.reject', $row) }}" class="d-inline">@csrf<input type="hidden" name="rejection_reason" value="Rejected by approver"><button class="btn btn-sm btn-outline-danger">Reject</button></form>@elseif($row->status === 'approved')<form method="POST" action="{{ route('office-supplies.issuances.issue', $row) }}" class="d-inline">@csrf<button class="btn btn-sm btn-outline-primary">Release</button></form>@endif</td></tr>@empty<tr><td colspan="6" class="text-center text-muted">No issuance records.</td></tr>@endforelse</tbody></table></div><div class="card-footer">{{ $issuances->links() }}</div></div>
@endsection
